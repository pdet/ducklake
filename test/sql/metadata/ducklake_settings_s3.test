# name: test/sql/metadata/ducklake_settings_s3.test
# description: Test ducklake_settings function with an S3 data path
# group: [metadata]

require ducklake

require parquet

require httpfs

test-env DUCKLAKE_CONNECTION {TEST_DIR}/{UUID}.db

test-env DATA_PATH {TEST_DIR}

require-env S3_TEST_SERVER_AVAILABLE 1

statement ok
ATTACH 'ducklake:{DUCKLAKE_CONNECTION}' AS ducklake (DATA_PATH '{DATA_PATH}/files')

statement ok
use ducklake

# Verify the data path is S3
query I
SELECT data_path LIKE 's3://%' FROM settings();
----
1
